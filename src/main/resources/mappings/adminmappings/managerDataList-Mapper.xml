<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="admin.datalist.dao.ManagerDataListDAO">
		
	<select id="getBooking" resultType="booking">
		select * from booking
	</select>
	
	<select id="getBookingInfo" resultType="booking">
		select booking_code, hotels_code, sort_type, room_code, member_id,
		booking_check_in, booking_check_out,
		booking_name, booking_phone, booking_cnt, 
        booking_date, booking_cancel_date,
		booking_total_price, booking_status
    		from booking
    		where booking_code = #{bookingCode}
	</select>
	
	<update id="setBookingInfo" parameterType="booking">
		update booking
			set room_code = #{roomCode},
				sort_type = #{sortType},
				booking_check_in = #{bookingCheckIn},
				booking_check_out = #{bookingCheckOut},
				booking_name = #{bookingName},
				booking_phone = #{bookingPhone},
				booking_cnt = #{bookingCnt},
				booking_total_price = #{bookingTotalPrice},
				booking_status = #{bookingStatus}
			where booking_code = #{bookingCode}
	</update>
	
	<select id="getListHotels" resultType="ManagerHotels">
		SELECT *
		FROM hotels
	</select>

	<select id="getHotels" resultType="ManagerHotels">
	SELECT * 
	FROM hotels
	WHERE hotels_code=#{hotelsCode}
	</select>
	
	<update id="updateHotel">
	UPDATE hotels 
	SET hotels_name=#{hotelsName}, hotels_type=#{hotelsType},
	hotels_phone=#{hotelsPhone}, hotels_zipcode=#{hotelsZipcode},
	hotels_address1=#{hotelsAddress1},hotels_address2=#{hotelsAddress2},
	hotels_path=#{hotelsPath}, hotels_info=#{hotelsInfo},
	hotels_introduce=#{hotelsIntroduce} where hotels_code=#{hotelsCode}
	</update>

</mapper>
