<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
        
<mapper namespace="client.member.vo.MemberVO">
    <insert id="insertMember" parameterType="member">
        insert into member (
        member_id
        ,member_pass
        ,member_name
        ,member_email
        ,member_phone
        ,member_status) values(
        #{memberId}
        ,#{memberPass}
        ,#{memberName}
        ,#{memberEmail}
        ,#{memberPhone}
        ,#{memberStatus})
    </insert>
    
    <select id="getId" resultType="int" parameterType="member">
        select count(*) from member where member_id = #{memberId}
    </select>
</mapper>

